<div class="resume-block">
    <h1>
        <FaIcon @prefix="fal" @icon={{@icon}} class="icon-large" />
        {{t (concat "main." @type )}}</h1>
    {{yield (hash work=(component "work-item")
                  education=(component "education-item")
                  )}}
</div>